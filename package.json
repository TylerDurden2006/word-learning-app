{
  "name": "wordforge",
  "version": "1.0.0",
  "private": true,
  "description": "WordForge manual dictionary with active recall and spaced repetition.",
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build-vercel.mjs",
    "dev": "node server.js",
    "start": "node server.js",
    "convex:dev": "convex dev",
    "convex:codegen": "convex codegen",
    "convex:deploy": "convex deploy",
    "convex:migrate-legacy": "convex run data:migrateLegacyWords",
    "migrate:convex": "node scripts/migrate-json-to-convex.mjs",
    "test:core": "node core.test.js",
    "test:convex": "vitest run",
    "test": "npm run test:core && npm run test:convex"
  },
  "dependencies": {
    "convex": "^1.37.0"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@types/node": "^25.6.0",
    "convex-test": "^0.0.51",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
